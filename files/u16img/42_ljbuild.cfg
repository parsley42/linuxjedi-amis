system_info:
   default_user:
     name: build
     homedir: /build
     lock_passwd: True
     gecos: LinuxJedi Cloud User for Ubuntu
     groups: [adm, audio, cdrom, dialout, dip, floppy, lxd, netdev, plugdev, sudo, wheel, video]
     sudo: ["ALL=(ALL) NOPASSWD:ALL"]
     shell: /bin/bash
